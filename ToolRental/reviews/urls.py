from django.urls import path
from . import views

app_name = 'reviews'

urlpatterns = [
    # Ces URLs seront à compléter quand les vues seront implémentées
    # Exemples basés sur les modèles :
    # path('create/<int:reservation_id>/', views.create_review, name='create'),
    # path('tool/<int:tool_id>/', views.tool_reviews, name='tool_reviews'),
    # path('<int:review_id>/', views.review_detail, name='detail'),
]